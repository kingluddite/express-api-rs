const router = require("express").Router();
const fs = require("fs");

// http://localhost:3000/api/characters
// router.get("/characters", (req, res) => {
//   // console.log("comedy is a funny thing");
// });
